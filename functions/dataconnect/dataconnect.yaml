specVersion: "v1"
serviceId: "functions"
location: "europe-west2"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "functions-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
